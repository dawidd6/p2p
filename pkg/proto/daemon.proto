syntax = "proto3";

option go_package = ".;proto";

import "pkg/proto/torrent.proto";

service Daemon {
    rpc Add (AddRequest) returns (AddResponse);
}

message AddRequest {
    Torrent torrent = 1;
}

message AddResponse {
    Torrent torrent = 1;
}

